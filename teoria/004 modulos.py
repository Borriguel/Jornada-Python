# para importar todas as funcionalidades de um módulo import + (nome do módulo).
# para importar as funcionalidades de um determinado módulo from ( nome do módulo) import (funcionalidade).
from math import sqrt
num = int(input('digite um número '))
print('a raíz quadrada desse número é {}'.format(sqrt(num)))
